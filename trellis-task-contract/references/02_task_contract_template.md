# Trellis Task Contract Template

复制下面模板生成 Trellis 任务。不要删除核心章节；如果某节不适用，必须说明“不适用原因”。

```markdown
# Task: <任务名称>

## 1. 背景与真实目标

用户真正想解决的问题是：

本任务的真实价值是：

不是：

## 2. 为什么不能简化成普通 MVP

如果 Agent 偷懒，可能会把任务简化成：

这个简化为什么不满足目标：

MVP 是否是用户明确要求：是 / 否

如果本任务是最小版本，它为什么仍然是完整闭环，而不是残缺壳子：

## 3. 本次最小完整闭环

本任务必须形成一个完整薄片：

- 输入：
- 处理：
- 输出：
- 接入点：
- 可验证结果：
- 用户/调用方能获得的真实价值：

## 4. 非目标

本任务明确不做：

这些不做不会破坏本次闭环，因为：

## 5. 允许后置项

允许后置：

后置原因：

后置后不会破坏当前设计的证据：

## 6. 禁止降级项

不得：

- 用 TODO 代替：
- 用 placeholder 代替：
- 用 mock 代替：
- 只做 happy path：
- 只写文档不实现：
- 只写接口不接入：
- 把完整目标改成未经授权的 MVP：

## 7. 实现范围

必须新增/修改的模块：

必须接入的真实调用链：

必须保留的现有行为：

可能影响的旧逻辑：

## 8. 边界场景

至少覆盖：

1. 正常场景：
2. 缺失输入：
3. 非法输入：
4. 空数据：
5. 旧数据兼容：
6. 重复执行：
7. 错误恢复：
8. 用户中断或部分完成：

如果某项不适用，说明原因：

## 9. 验收标准

### 功能验收

- [ ] 

### 测试验收

- [ ] 

### 文档验收

- [ ] 

### 兼容性验收

- [ ] 

### 回归验收

- [ ] 

## 10. 反偷懒验收

- [ ] 没有把完整目标降级成未经授权的 MVP。
- [ ] 没有用 TODO / placeholder / mock 冒充完成。
- [ ] 没有只实现 happy path。
- [ ] 没有只写接口而不接入真实调用链。
- [ ] 没有只更新文档而不完成代码，除非本任务明确是文档任务。
- [ ] 每个验收项都有证据来源。
- [ ] 允许后置项不会破坏本次闭环。
- [ ] 禁止降级项全部满足。

## 11. 证据计划

完成后必须提供：

- 修改文件列表：
- 新增/更新测试：
- 运行命令：
- 命令输出摘要：
- 手动验证步骤：
- 截图/日志/产物路径：
- 未覆盖风险：

## 12. Checkpoints

### Checkpoint 1: <名称>

完成条件：

证据：

不得进入下一步的阻塞条件：

### Checkpoint 2: <名称>

完成条件：

证据：

不得进入下一步的阻塞条件：

### Checkpoint 3: <名称>

完成条件：

证据：

不得进入下一步的阻塞条件：

## 13. Review 打回条件

出现以下任一情况必须打回：

- 任务被降级成未授权 MVP。
- 有 TODO / placeholder / mock 冒充完成。
- 没有测试或证据。
- 只覆盖 happy path。
- 没有接入真实调用链。
- 验收标准无法独立判断。
- 与用户真实目标不一致。
- 缺少边界场景。
- 允许后置项实际破坏本次闭环。

## 14. 最终完成报告格式

实现完成后必须回填：

- 完成摘要：
- 修改文件：
- 新增/更新测试：
- 运行命令与结果：
- 验收项对应证据：
- 未完成项与原因：
- 风险与后续建议：
```
